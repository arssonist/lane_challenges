this.state = {
  historyvar originalColor = color: [255, 255, 255, 0.6],
  if(color.state != originalColor){
    change

  }
  scale: 1.2,
  rotate: 0,
}

onChange=


var history = [];
var current =
when state changes for above props, print string
